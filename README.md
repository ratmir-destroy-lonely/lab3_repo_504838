# Лабораторная работа №3  
**Система контроля версий Git**  

🎯 **Цель работы:** Освоить основные операции Git  
👨‍💻 **Студент:** Ратмир  
🔢 **ИСУ:** 504838  
📅 **Дата:** 2024 год  

## 📋 Задания выполнены:
- ✅ Работа с коммитами и ветками
- ✅ Merge и разрешение конфликтов  
- ✅ Rebase веток
- ✅ Создание release-ветки
- ✅ Сравнение коммитов (diff)

## 🛠️ Технологии:
- Git
- GitHub

## 📁 Структура репозитория:
```
lab3_repo_504838/
├── run.txt          # Основной файл с изменениями
├── diff.txt         # Сравнение коммитов
├── history.txt      # История коммитов
└── README.md        # Этот файл
```

## 📊 Статистика:
![GitHub repo size](https://img.shields.io/github/repo-size/ratmir-destroy-lonely/lab3_repo_504838)
![GitHub last commit](https://img.shields.io/github/last-commit/ratmir-destroy-lonely/lab3_repo_504838)

---
*Лабораторная работа выполнена*



---

# Отчёт по лабораторной работе №3
## "Система контроля версий Git"
**Студент:** Ратмир  
**ИСУ:** 504838  
**Дата выполнения:** 20.11.2024

---

## Введение
**Цель работы:** Освоить основные операции системы контроля версий Git, включая работу с коммитами, ветками, разрешением конфликтов и взаимодействием с удалённым репозиторием.

---

## Ход работы

### 1. Подготовка окружения

**Нужно было:** Установить необходимые инструменты для работы

```bash
sudo apt install git
sudo apt install curl git
```

**Проблема:** Изначально в системе отсутствовали curl и git
**Решение:** Я установил необходимые пакеты через менеджер пакетов apt

### 2. Настройка Git

**Нужно было:** Настроить пользователя для создания коммитов

```bash
git config --global user.name "Ratmir"
git config --global user.email "********@*****.**"
```

**Результат:** Я настроил имя и email для идентификации автора коммитов

### 3. Создание репозитория

**Нужно было:** Загрузить и настроить лабораторный репозиторий

```bash
curl -s https://raw.githubusercontent.com/itregernium/avs_lab3/refs/heads/main/lab3gen.sh | sh -s -- 504838
```

**Результат:** Создал папку `lab3_repo_504838` с начальной структурой проекта

### 4. Задание 1: Первый коммит

**Нужно было:** Добавить строку "тест" в файл run.txt

```bash
echo "тест" >> run.txt
git add run.txt
git commit -m "Добавлена строка 'тест'"
```

**Результат:** Я создал первый коммит с хешем `72f1c33`

### 5. Задание 2: Второй коммит

**Нужно было:** Добавить строку "не тест" в файл run.txt

```bash
echo "не тест" >> run.txt
git add run.txt
git commit -m "Добавлена строка 'не тест'"
```

**Результат:** Я создал второй коммит с хешем `2c8403a`

### 6. Задание 3: Работа с веткой gguf

**Нужно было:** Создать ветку gguf и добавить изменения

```bash
git checkout gguf
echo "тест" >> run.txt
git add run.txt
git commit -m "Добавлена строка 'тест' в ветке gguf"
```

**Проблема:** Ветка gguf уже существовала
**Решение:** Использовал команду `git checkout gguf` вместо `git checkout -b gguf`

### 7. Задание 4: Сравнение коммитов

**Нужно было:** Сохранить разницу между коммитами в файл

```bash
git diff 7a30607 766f0ab > diff.txt
```

**Результат:** Я создал файл `diff.txt` с различиями между коммитами

### 8. Задание 5: Откат к 3-му коммиту

**Нужно было:** Вернуться к 3-му коммиту и создать новую ветку

```bash
git checkout 7a30607
git checkout -b temp_branch
echo "тест" >> run.txt
git commit -m "Новый коммит после отката к 3-му коммиту"
```

**Особенность:** Я работал в состоянии "detached HEAD" с последующим созданием ветки

### 9. Задание 6: Rebase ветки gguf

**Нужно было:** Перебазировать ветку gguf на main

```bash
git checkout gguf
git rebase main
```

**Результат:** Успешно выполнил rebase без конфликтов

### 10. Задание 7: Merge веток feature1 и feature2

**Нужно было:** Объединить ветки feature1 и feature2 в main

```bash
git checkout main
git merge feature1
# Разрешение конфликта в run.txt
git merge feature2
# Разрешение второго конфликта
```

**Проблемы:** 
- Возникли конфликты слияния в файле run.txt
- Автоматический merge не сработал

**Решение:** Я вручную отредактировал файл run.txt в редакторе nano, убрав маркеры конфликта и оставив нужное содержимое, после чего выполнил коммит

### 11. Задание 8: Создание release ветки

**Нужно было:** Создать ветку release и объединить все изменения

```bash
git checkout -b release
git merge gguf hotfix backend_cuda backend_metal hf_tests server_last pr_feature feature1 feature2 temp_branch
```

**Проблема:** Возник конфликт при merge многих веток
**Решение:** Снова вручную разрешил конфликт в run.txt, отредактировав файл и убрав маркеры конфликта

### 12. Сохранение истории

**Нужно было:** Сохранить историю коммитов в файл

```bash
git log --oneline --graph --all > history.txt
```

### 13. Работа с GitHub

**Нужно было:**
- Создать репозиторий на GitHub
- Настроить удалённый origin
- Запушить все ветки

```bash
git remote add origin https://github.com/ratmir-destroy-lonely/lab3_repo_504838.git
git push -u origin main
git push --all origin
```

**Проблема:** Потребовалась аутентификация через Personal Access Token
**Решение:**  Создал токен в настройках GitHub и использовал его вместо пароля для аутентификации

---

## Проблемы и их решения

1. **Отсутствие curl и git** - установил необходимые пакеты через apt
2. **Ненастроенный пользователь Git** - я настроил имя и email в конфигурации Git
3. **Существующие ветки** - я использовал checkout вместо checkout -b для переключения на существующие ветки
4. **Конфликты слияния** - вручную редактировал файлы в nano для разрешения конфликтов
5. **Аутентификация на GitHub** - создал и использовал Personal Access Token вместо пароля
6. **Отсутствие ветки release на GitHub** - я явно запушил ветку release командой git push origin release

---

## Выводы

### Чему я научился:
1. **Базовые операции Git:** add, commit, status, log
2. **Работа с ветками:** создание, переключение, удаление
3. **Слияние изменений:** merge, rebase, разрешение конфликтов
4. **История коммитов:** просмотр, сравнение, навигация
5. **Работа с удалённым репозиторием:** push, pull, remote
6. **Интеграция с GitHub:** создание репозитория, настройка доступа

### Приобретённые навыки:
- Уверенная работа в командной строке с Git
- Навыки разрешения конфликтов слияния
- Понимание workflow разработки с использованием веток
- Опыт работы с удалёнными репозиториями
- Умение оформлять и документировать проекты

### Заключение:
Я успешно выполнил лабораторную работу. Все задания реализованы, я создал полнофункциональный Git-репозиторий с историей изменений и разместил его на GitHub. Я приобрёл практические навыки работы с системой контроля версий, которые будут полезны мне в дальнейшей учебной и профессиональной деятельности.
